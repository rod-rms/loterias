root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

# Markdown occasionally uses two trailing spaces for a hard line break;
# never strip trailing whitespace there.
[*.md]
trim_trailing_whitespace = false
